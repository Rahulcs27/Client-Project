import { CompanyMasterUpdateDto } from './company-master-update-dto';

describe('CompanyMasterUpdateDto', () => {
  it('should create an instance', () => {
    expect(new CompanyMasterUpdateDto()).toBeTruthy();
  });
});
