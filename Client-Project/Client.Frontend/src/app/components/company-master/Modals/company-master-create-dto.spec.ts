import { CompanyMasterCreateDto } from './company-master-create-dto';

describe('CompanyMasterCreateDto', () => {
  it('should create an instance', () => {
    expect(new CompanyMasterCreateDto()).toBeTruthy();
  });
});
